{
  "name": "web-components-presentation",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "dvlp": "*"
  },
  "scripts": {
    "dev": "dvlp --silent",
    "open": "open -a \"Google Chrome\" http://localhost:8080?showtime",
    "start": "pnpm run open && pnpm run dev -- --silent"
  }
}
